// custom JS code 
$(document).ready( function(){
	// startDate and endDate deskBooking
	$('#startDate-deskBooking').datepicker({
		autoclose: true,
		format: "dd/mm/yyyy"
	});
	$('#endDate-deskBooking').datepicker({
		autoclose: true,
		format: "dd/mm/yyyy"
	});
	

	// startDate and endDate employee
	$('#startDate-employee').datepicker({
		autoclose: true,
		format: "dd/mm/yyyy"
	});
	$('#endDate-employee').datepicker({
		autoclose: true,
		format: "dd/mm/yyyy"
	});


	// startDate and endDate carBooking
	$('#startDate-carBooking').datepicker({
		autoclose: true,
		format: "dd/mm/yyyy"
	});
	$('#endDate-carBooking').datepicker({
		autoclose: true,
		format: "dd/mm/yyyy"
	});

	// startDate and endDate equipmentTracking
	$('#startDate-equipmentTracking').datepicker({
		autoclose: true,
		format: "dd/mm/yyyy"
	});
	$('#endDate-equipmentTracking').datepicker({
		autoclose: true,
		format: "dd/mm/yyyy"
	});



});